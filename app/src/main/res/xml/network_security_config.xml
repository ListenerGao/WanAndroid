<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
    从Android 9.0（API级别28）开始，默认情况下限制了明文流量的网络请求，
    对未加密流量不再信任，直接放弃请求，因此http的url均无法在WebView中加载，
    https 不受影响。
    -->
    <base-config cleartextTrafficPermitted="true"/>
</network-security-config>